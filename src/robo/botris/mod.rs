/// Things in this module SHOULD be decoupled from the library into the botris binary.
/// In the meantime, these types and structs will live here as an artifact from initial development
pub mod game_info;
pub mod types;
